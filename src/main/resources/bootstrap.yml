spring:
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
     profiles: subject
     application:
       name: random-subject-generator

---
spring:
     profiles: predicate
     application:
       name: random-predicate-generator

---
spring:
     profiles: object
     application:
       name: random-object-generator
